# Local Services

```sh
cp .env.example .env
docker compose up -d
```

Included services:

- PostgreSQL on `localhost:5432`
- Redis on `localhost:6379`
- Valkey on `localhost:6380`
- MySQL on `localhost:3306`
- Mailpit SMTP on `localhost:1025`
- Mailpit UI on `http://localhost:8025`
